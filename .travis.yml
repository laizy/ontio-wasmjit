language: rust
rust:
  - nightly
sudo: false
cache: cargo

before_script:
- rustup component add rustfmt
script:
  - cargo build --release --all

deploy:
  provider: releases
  token: "${RELEASES_TOKEN}"
  file_glob: true
  file: target/release/lib*.a
  skip_cleanup: true
  draft: true
